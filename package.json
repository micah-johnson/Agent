{
  "name": "agent",
  "version": "0.1.0",
  "description": "Personal AI agent. Lives in Slack. Writes code, runs tasks, remembers everything.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "setup": "bun run src/cli/setup.ts",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "keywords": ["slack", "ai", "agent", "claude"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@mariozechner/pi-ai": "^0.54.0",
    "@slack/bolt": "^4.2.0",
    "chokidar": "^5.0.0",
    "puppeteer": "^24.37.2",
    "sqlite-vec": "^0.1.7-alpha.2",
    "voyageai": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "bun-types": "^1.3.8"
  }
}
